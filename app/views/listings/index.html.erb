<div class="banner mb-4">
</div>

<div class="container">
  <% if @query %>
    <%= render 'query' %>
  <% end %>

  <div class="row">
    <% if @listings.empty? %>
      <p class="pl-4"><em>0 results found</em></p>
    <% else %>
      <%= render @records %>
    <% end %>
  </div>

  <div class="d-flex justify-content-center">
    <%= render partial: 'pagy/bootstrap_nav', locals: {pagy: @pagy} %>
  </div>
</div>
